import App from 'App';
import React from 'react';
import ReactDOM from 'react-dom';
import 'antd/dist/antd.css';

(async function () {
  document.title = 'Pet safe';
  ReactDOM.render(<App />, document.getElementById('root'));
})();
